let initialState = {
    friends: [
        {id:1, name:"Vanya"},
        {id:2, name:"Sveta"},
        {id:3, name:"Kostya"}
    ]
}

const SidebarReducer = (state = initialState, action) =>{


    return state;
}

export default SidebarReducer